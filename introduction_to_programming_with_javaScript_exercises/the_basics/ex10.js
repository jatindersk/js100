'foo' === 'Foo'

// The above expression evaluates to false as the first letter of the operands has a different case.

// LS explanation:
// It evaluates as false since case matters when comparing strings.