let y = "5"
console.log(y++); // 5

// It returns numeric 5. This is because the post increment operator
// coerces the operand to numeric data type. Since, it is a post
// increment operator, it returns the original value of the variable i.e 5