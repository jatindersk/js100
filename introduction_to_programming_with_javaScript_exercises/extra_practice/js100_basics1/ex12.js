console.log(greeting);

var greeting = 'Hello world!';

// It will log undefined. This is because all the variables declared
// with var keyword are hoisted that is they are moved to the beginning
// of the scope.